const express = require('express') // calling the express package through the node modules after it being imported through node package manager (npm) 

const server = express() // creating a server instance 

server.listen(3000) // starting the server instance on the port 3000